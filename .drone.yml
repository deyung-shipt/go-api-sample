kind: pipeline
name: test

workspace:
  base: /go
  path: src/github.com/deyung-shipt/${DRONE_REPO_NAME}

trigger:
  event:
    - push
    - pull_request
    - tag

steps:
  - name: test
    image: golang:latest
    commands:
      - go test -coverprofile=coverage.txt ./...
  